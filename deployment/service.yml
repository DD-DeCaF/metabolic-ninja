kind: Service
apiVersion: v1
metadata:
  name: metabolic-ninja-caffeine
spec:
  selector:
    app: metabolic-ninja
    env: caffeine
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000
